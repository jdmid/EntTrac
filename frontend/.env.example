# Copy this to .env.local for local development (already gitignored).
# Points the frontend at your local backend. In production, leave this
# unset — the app falls back to a relative '/api' path, which resolves
# correctly since Spring Boot serves the frontend and API same-origin.
VITE_API_URL=http://localhost:8080/api